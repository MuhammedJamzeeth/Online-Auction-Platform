server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/auction_platform
    username: root
    password: jamzee
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect
